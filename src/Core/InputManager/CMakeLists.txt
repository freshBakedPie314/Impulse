file(GLOB_RECURSE INPUT_SOURCES CONFIGURE_DEPENDS ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp)

add_library(InputManager STATIC ${INPUT_SOURCES})

target_include_directories(InputManager PUBLIC 
	${CMAKE_CURRENT_SOURCE_DIR}
)