file(GLOB_RECURSE WINDOW_SOURCES CONFIGURE_DEPENDS ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp)

add_library(Window STATIC ${WINDOW_SOURCES})

target_include_directories(Window PUBLIC 
	${CMAKE_CURRENT_SOURCE_DIR}
)